using System;

namespace Scripts
{
	// The project already contains a client script file. That is 
	// why this script is empty. You can write code in here,
	// but it should be called from the main script.
}